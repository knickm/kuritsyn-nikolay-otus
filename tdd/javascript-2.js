/**
 * Возвращает уникальный селектор к элементу в DOM
 * так что вызов document.querySelectorAll(getPath($0)).length === 1 если $0 является HTML-элементом в DOM
 * @param {HTMLElement} node
 */
function getPath(node) {
    return null;
}
