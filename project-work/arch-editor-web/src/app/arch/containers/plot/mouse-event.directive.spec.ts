import { MouseEventDirective } from './mouse-event.directive';

describe('MoseEventDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseEventDirective();
    expect(directive).toBeTruthy();
  });
});
